const { describe, it } = require("node:test");
const assert = require("node:assert");

describe("REST API with CRUD", () => {
  it("should pass placeholder test", () => {
    assert.strictEqual(true, true);
  });
});
